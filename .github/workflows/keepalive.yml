name: Keep CreepCast Alive
on:
  schedule:
    - cron: '*/20 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HuggingFace Space
        run: curl -s -o /dev/null -w "%{http_code}" https://drCreepyData-creepcast-story-finder.hf.space
